import { ReverseIfDirective } from './reverse-if.directive';

describe('ReverseIfDirective', () => {
  it('should create an instance', () => {
    const directive = new ReverseIfDirective();
    expect(directive).toBeTruthy();
  });
});
