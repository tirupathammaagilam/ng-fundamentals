import { HighlightV2Directive } from './highlight-v2.directive';

describe('HighlightV2Directive', () => {
  it('should create an instance', () => {
    const directive = new HighlightV2Directive();
    expect(directive).toBeTruthy();
  });
});
