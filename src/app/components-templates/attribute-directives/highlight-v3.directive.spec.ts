import { HighlightV3Directive } from './highlight-v3.directive';

describe('HighlightV3Directive', () => {
  it('should create an instance', () => {
    const directive = new HighlightV3Directive();
    expect(directive).toBeTruthy();
  });
});
