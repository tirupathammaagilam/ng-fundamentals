import { HighlightV1Directive } from './highlight-v1.directive';

describe('HighlightV1Directive', () => {
  it('should create an instance', () => {
    const directive = new HighlightV1Directive();
    expect(directive).toBeTruthy();
  });
});
