import { FlyingHeroesPurePipe } from './flying-heroes-pure.pipe';

describe('FlyingHeroesPurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingHeroesPurePipe();
    expect(pipe).toBeTruthy();
  });
});
